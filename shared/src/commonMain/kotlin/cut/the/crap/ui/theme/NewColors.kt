package cut.the.crap.ui.theme

import androidx.compose.ui.graphics.Color

// Modern, Sophisticated Color Palette

// Primary Colors - Refined Blues
val PrimaryBlueDark = Color(0xFF4A90E2)        // Softer, muted blue (easier on eyes)
val PrimaryBlueLight = Color(0xFF2B5B8F)       // Deep, professional blue
val PrimaryBlueLighter = Color(0xFF6BA5E7)     // Lighter variant for containers

// Secondary Colors - Warm Accents
val SecondaryAmberDark = Color(0xFFE8A547)     // Muted amber (less harsh)
val SecondaryAmberLight = Color(0xFFD68910)    // Rich golden amber
val SecondaryAmberLighter = Color(0xFFF5C76B)  // Soft amber for containers

// Tertiary Colors - Elegant Rose
val TertiaryRoseDark = Color(0xFFD8647C)       // Soft rose/pink
val TertiaryRoseLight = Color(0xFFB13A5E)      // Deep rose
val TertiaryRoseLighter = Color(0xFFE896AA)    // Light rose for containers

// Dark Theme Surfaces - Enhanced Depth
val DarkBackground = Color(0xFF0F0F0F)          // Slightly darker for better contrast
val DarkSurface = Color(0xFF1A1A1A)             // Refined dark surface
val DarkSurfaceVariant = Color(0xFF252525)      // Slightly lighter variant
val DarkSurfaceElevated = Color(0xFF2A2A2A)     // For elevated cards
val DarkSurfaceBright = Color(0xFF303030)       // Brightest surface level

// Light Theme Surfaces - Clean & Modern
val LightBackground = Color(0xFFF5F5F5)         // Light gray background for contrast
val LightSurface = Color(0xFFFFFFFF)            // Pure white for cards
val LightSurfaceVariant = Color(0xFFE8E8E8)     // Medium gray variant for contrast
val LightSurfaceDim = Color(0xFFDCDCDC)         // Dimmed surface for distinction
val LightSurfaceContainer = Color(0xFFEEEEEE)   // Container background with better contrast

// Outline Colors
val DarkOutline = Color(0xFF3D3D3D)             // Subtle dark outlines
val DarkOutlineVariant = Color(0xFF2A2A2A)      // Even more subtle
val LightOutline = Color(0xFFCACACA)            // Refined light outlines
val LightOutlineVariant = Color(0xFFE0E0E0)     // Lighter variant

// Error Colors
val DarkError = Color(0xFFE57373)               // Softer error red
val DarkErrorContainer = Color(0xFF5D2A2A)      // Error container
val LightError = Color(0xFFD32F2F)              // Clear error indication
val LightErrorContainer = Color(0xFFFFDAD6)     // Light error background

// Scrim Colors
val DarkScrim = Color(0x88000000)               // Modal overlays
val LightScrim = Color(0x52000000)              // Modal overlays

// Inverse Colors
val DarkInverseSurface = Color(0xFFE8EAED)
val DarkInverseOnSurface = Color(0xFF1A1A1A)
val LightInverseSurface = Color(0xFF2A2A2A)
val LightInverseOnSurface = Color(0xFFFAFAFA)